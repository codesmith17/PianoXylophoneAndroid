<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Pianofinal" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="black_key_style">
        <item name="android:background">@drawable/pressed_black_key</item>
    </style>

    <style name="white_key_style">
        <item name="android:background">@drawable/pressed_white_key</item>
    </style>

    <style name="KeyStyle">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_marginTop">3dp</item>
        <item name="android:layout_marginBottom">3dp</item>
        <item name="android:bottomRightRadius">100dp</item>
        <item name="android:bottomLeftRadius">100dp</item>
        <item name="android:topLeftRadius">100dp</item>
        <item name="android:topRightRadius">100dp</item>
    </style>
</resources>
